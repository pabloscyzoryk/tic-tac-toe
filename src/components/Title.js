// styles
import './Title.css'

export default function Title({ title }) {

    return (
        <div className="title-wrapper">
            <h1 className="title">{title}</h1> 
        </div>
  )
}
